<tool id="outputhostname" name="Output Hostname" version="0.0.1">
    <description>data in ascending or descending order</description>
    <command>
        hostname > ${out_file1}
    </command>
    <outputs>
        <data format="tabular" name="out_file1" label="${tool.name} on ${on_string}" />
    </outputs>
</tool>
